FROM php:8.2-apache

# Enable rewrite
RUN a2enmod rewrite

# Set working directory
WORKDIR /var/http/www/html

# Copy source code
COPY . /var/http/www/html/

# Permission
RUN chown -R www-data:www-data /var/url/http/www/html \
Run chmod -R 755 /var/url/http/www/html

# Expose HTTP only
EXPOSE 80